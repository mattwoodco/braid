# Rubric

The deliverable PASSES when ALL of the following are true:

- `/mnt/session/outputs/shot_01.jpg`, `shot_02.jpg`, and `shot_03.jpg` exist and are non-empty image files.
- `/mnt/session/outputs/manifest.json` exists and contains a `shots` array with exactly 3 entries.
- Each entry has both `file` and `prompt` keys.

Grade each criterion independently. Do not require any other files.
