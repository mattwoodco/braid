# Banned terms — {{brand_name}}

Words and phrases that must NOT appear in any prompt, copy, or rendered text.
The critic auto-fails any shot whose prompt contains one of these.

{{#each banned_terms}}- {{this}}
{{/each}}
